workflow:
  serviceAccount:
    create: true
    name: "workflows"
  rbac:
    create: true

controller:
  containerRuntimeExecutor: "emissary"
  workflowNamespaces:
    - workflows

server:
  ingress:
    enabled: true
    ingressClassName: "nginx"
    hosts:
      - workflows.{{ .Values.domain }}
